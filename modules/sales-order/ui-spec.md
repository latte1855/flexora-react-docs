# Sales Order Module – UI Spec

計劃沿用報價 Workspace 的設計語言：

1. **SO Workspace**：左側 Filter Hub（預設/自訂條件），右側 List + Pipeline（如需）。  
2. **Detail Drawer**：顯示訂單摘要、Workflow 動作、行項表格、文件紀錄。  
3. **全頁編輯 / 建立**：與報價整單編輯風格一致。

## TODO

- [ ] 將既有 Sales Order wireframe / Figma 圖貼至此。
- [ ] 列出欄位（行項、交期、付款、PriceList 連動）。
- [ ] 補上 Workflow Interaction（送審、開啟 Workflow、查看歷程）。
